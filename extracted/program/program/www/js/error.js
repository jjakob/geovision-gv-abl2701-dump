// JavaScript Document
GlobalInvoke(window);
var msg = getparastr("ErrorMsg");

// 加载语言文件
loadlanguageFile(top.language);

$(document).ready(function() {
    // 初始化语言标签
        initLang();
    });